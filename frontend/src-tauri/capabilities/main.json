{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main Streamarr desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "core:app:default", "opener:allow-default-urls"]
}
