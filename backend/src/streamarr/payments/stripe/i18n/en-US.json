{
  "name": "Stripe",
  "description": "Payment processing via Stripe for subscription management",
  "config": {
    "secret_key": {
      "label": "Secret API Key",
      "hint": "Stripe secret API key"
    },
    "publishable_key": {
      "label": "Publishable API Key",
      "hint": "Stripe publishable API key"
    },
    "webhook_secret": {
      "label": "Webhook Secret",
      "hint": "Stripe webhook signing secret"
    }
  }
}
